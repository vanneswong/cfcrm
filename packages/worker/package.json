{
  "name": "@crm/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute crm-db --file=../../migrations/0001_init.sql",
    "db:migrate:local": "wrangler d1 execute crm-db --local --file=../../migrations/0001_init.sql"
  },
  "dependencies": {
    "hono": "^4.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.10.0"
  }
}
